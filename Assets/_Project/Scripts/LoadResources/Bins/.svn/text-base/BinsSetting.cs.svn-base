using UnityEngine;
using System.Collections;
using System.Collections.Generic;
using System;

public class BinsSetting {

	public static void setBins(out List<string> binNames,out List<Type> types,out List<int> loadWays)
	{
		binNames = new List<string> ();
		types = new List<Type> ();
		loadWays = new List<int> ();

		//============================第一种加载方式============================//
//		binNames.Add("text.bin");
//		types.Add(typeof(TextData));
//		loadWays.Add(1);

		binNames.Add ("item.bin");
		types.Add (typeof(ItemData));
		loadWays.Add (1);

		binNames.Add ("effect.bin");
		types.Add (typeof(EffectData));
		loadWays.Add (1);

		binNames.Add ("skill.bin");
		types.Add (typeof(SkillData));
		loadWays.Add (1);

//        binNames.Add("levelup.bin");
//        types.Add(typeof(LevelUpData));
//        loadWays.Add(1);

		//============================第二种加载方式============================//
//        binNames.Add("recharge.bin");
//        types.Add(typeof(RechargeData));
//        loadWays.Add(2);
	}

}
