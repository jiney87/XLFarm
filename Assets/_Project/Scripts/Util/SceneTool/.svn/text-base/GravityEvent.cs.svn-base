using UnityEngine;
using System.Collections;

public class GravityEvent : MonoBehaviour {

	public DrawBoundary boundary;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
//		if (BoatHandler.instance != null && BoatHandler.instance.isRunning) {
//			Vector3 acc=Input.acceleration;
//			float x=Mathf.Clamp(acc.x,-1,1);
//			//float y=Mathf.Clamp(acc.y,-1,1);
//			//float z=Mathf.Clamp(acc.z,-1,1);
//			//z=0;
//
//			Vector3 temp = transform.localPosition + Vector3.Lerp(Vector3.zero,new Vector3(x,0,0) * Time.deltaTime,0.9f)*2;
//			if (temp.x < boundary.x0) {
//				temp.x = boundary.x0;
//			}
//			else if (temp.x > boundary.x1) {
//				temp.x = boundary.x1;
//			}
//			transform.localPosition = temp;
//		}
	}
		
}
